{
  "lexicon": 1,
  "id": "li.dzming.nightboat.publication.entry",
  "description": "Cross-post record: the content author declares that their article or series is being published to the named publication. Written under the CONTENT AUTHOR's DID so that authors retain agency over where their content appears.",
  "defs": {
    "main": {
      "type": "record",
      "key": "tid",
      "record": {
        "type": "object",
        "required": ["publicationSlug", "contentUri", "contentKind", "createdAt"],
        "properties": {
          "publicationUri": {
            "type": "string",
            "format": "at-uri"
          },
          "publicationSlug": {
            "type": "string",
            "maxLength": 64
          },
          "contentUri": {
            "type": "string",
            "description": "For articles: their at-uri. For series: the series id (series don't have at-uris today)."
          },
          "contentKind": { "type": "string", "enum": ["article", "series"] },
          "createdAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
